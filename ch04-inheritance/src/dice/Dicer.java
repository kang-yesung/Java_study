package dice;

public class Dicer {

    public static void throwDice(Dice dice){

        int num = dice.getNumber();
        System.out.println(num);
        /*
        * - `throwDice(Dice dice)` 메서드를 가지고 있는 클래스
- 매개변수로 받은 `Dice` 객체의 `getNumber()`를 호출하여 값을 받아옴
- 해당 숫자를 **출력**함*/
    }
}
